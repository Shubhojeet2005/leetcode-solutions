# 678. Valid Parenthesis String
# Difficulty: Medium
# Tags: String, Dynamic Programming, Stack, Greedy, Bracket Sequences
# URL: https://leetcode.com/problems/valid-parenthesis-string/submissions/2098888734/
# Solved At: 2026-08-08T09:07:58.770Z
#
# Problem:
# Given a string s containing only three types of characters: '(', ')' and '*', return true if s is valid.
# 
# The following rules define a valid string:
# 
# 
# 	Any left parenthesis '(' must have a corresponding right parenthesis ')'.
# 	Any right parenthesis ')' must have a corresponding left parenthesis '('.
# 	Left parenthesis '(' must go before the corresponding right parenthesis ')'.
# 	'*' could be treated as a single right parenthesis ')' or a single left parenthesis '(' or an empty string "".
# 
# 
#  
# Example 1:
# Input: s = "()"
# Output: true
# Example 2:
# Input: s = "(*)"
# Output: true
# Example 3:
# Input: s = "(*))"
# Output: true
# 
#  
# Constraints:
# 
# 
# 	1 <= s.length <= 100
# 	s[i] is '(', ')' or '*'.

            }

            else { // '*'
                low--;   // '*' can be ')'
                high++;  // '*' can be '('
            }

            // Too many ')' even in the best case
            if (high < 0)
                return false;

            // low cannot be negative
            low = max(low, 0);
        }

        return low == 0;
    }
};
